/**
 * Platform constants — safe to import from Edge runtime (no node:crypto dependency).
 */

export const DEMO_OPERATOR_ID = "operator-demo";
export const DEMO_OPERATOR_IDS: string[] = [DEMO_OPERATOR_ID];
